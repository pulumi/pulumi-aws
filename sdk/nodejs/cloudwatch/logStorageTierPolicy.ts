// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages a CloudWatch Logs account-level storage tier policy. When set to `INTELLIGENT_TIERING`, CloudWatch Logs automatically moves log data to the most cost-effective storage tier based on access frequency.
 *
 * > Deletion of this resource will reset the storage tier policy to `STANDARD` (the default state).
 *
 * ## Example Usage
 *
 * ### Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.cloudwatch.LogStorageTierPolicy("example", {storageTier: "INTELLIGENT_TIERING"});
 * ```
 *
 * ## Import
 *
 * ### Identity Schema
 *
 * #### Required
 *
 * * `region` (String) Region where this resource is managed.
 *
 * #### Optional
 *
 * * `accountId` (String) AWS Account where this resource is managed.
 *
 * Using `pulumi import`, import storage tier policies using `region`. For example:
 *
 * ```sh
 * $ pulumi import aws:cloudwatch/logStorageTierPolicy:LogStorageTierPolicy example us-west-2
 * ```
 */
export class LogStorageTierPolicy extends pulumi.CustomResource {
    /**
     * Get an existing LogStorageTierPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: LogStorageTierPolicyState, opts?: pulumi.CustomResourceOptions): LogStorageTierPolicy {
        return new LogStorageTierPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aws:cloudwatch/logStorageTierPolicy:LogStorageTierPolicy';

    /**
     * Returns true if the given object is an instance of LogStorageTierPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is LogStorageTierPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === LogStorageTierPolicy.__pulumiType;
    }

    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    declare public readonly region: pulumi.Output<string>;
    /**
     * Storage tier to set for the account. Valid values are `STANDARD` or `INTELLIGENT_TIERING`.
     */
    declare public readonly storageTier: pulumi.Output<string>;

    /**
     * Create a LogStorageTierPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: LogStorageTierPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: LogStorageTierPolicyArgs | LogStorageTierPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as LogStorageTierPolicyState | undefined;
            resourceInputs["region"] = state?.region;
            resourceInputs["storageTier"] = state?.storageTier;
        } else {
            const args = argsOrState as LogStorageTierPolicyArgs | undefined;
            if (args?.storageTier === undefined && !opts.urn) {
                throw new Error("Missing required property 'storageTier'");
            }
            resourceInputs["region"] = args?.region;
            resourceInputs["storageTier"] = args?.storageTier;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(LogStorageTierPolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering LogStorageTierPolicy resources.
 */
export interface LogStorageTierPolicyState {
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * Storage tier to set for the account. Valid values are `STANDARD` or `INTELLIGENT_TIERING`.
     */
    storageTier?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a LogStorageTierPolicy resource.
 */
export interface LogStorageTierPolicyArgs {
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * Storage tier to set for the account. Valid values are `STANDARD` or `INTELLIGENT_TIERING`.
     */
    storageTier: pulumi.Input<string>;
}
