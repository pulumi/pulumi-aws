// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as enums from "../types/enums";
import * as utilities from "../utilities";

/**
 * Manages an Amazon Managed Streaming for Apache Kafka (MSK) Channel.
 *
 * With [Amazon MSK Data Delivery](https://docs.aws.amazon.com/msk/latest/developerguide/msk-data-delivery.html), a channel delivers Apache Kafka data from an Amazon MSK Provisioned cluster that uses Express brokers directly to Amazon S3, without connectors or additional infrastructure to manage. A channel reads records from a Kafka topic and delivers them to one of two destinations:
 *
 * * **Amazon S3 general purpose buckets** — records are written in their source format (`JSON`, `ByteArray`, or `String`) as objects, for use cases such as log archival, compliance retention, and Kafka replay.
 * * **Apache Iceberg tables on Amazon S3 Tables** — `JSON` records, validated against an AWS Glue Schema Registry schema, are materialized as Apache Iceberg tables.
 *
 * Records that cannot be processed are routed to a required dead-letter queue (DLQ) Amazon S3 bucket. Channels are supported only on Amazon MSK Provisioned clusters that use Express brokers; Standard brokers and Amazon MSK Serverless are not supported.
 *
 * > **Note:** A channel does not backfill previously produced data; only data produced after creation is delivered. For the Iceberg destination, a channel creates a new Iceberg table for each configuration.
 *
 * ## Example Usage
 *
 * ### Amazon S3 Destination
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.msk.Channel("example", {
 *     topicConfiguration: {
 *         recordConverter: {
 *             valueConverter: "BYTE_ARRAY",
 *         },
 *         topicArn: exampleAwsMskTopic.arn,
 *     },
 *     s3Destination: {
 *         deadLetterQueueS3: {
 *             bucketArn: dlq.arn,
 *         },
 *         storage: {
 *             bucketArn: exampleAwsS3Bucket.arn,
 *             compressionType: "NONE",
 *             storageClass: "STANDARD",
 *         },
 *         serviceExecutionRoleArn: exampleAwsIamRole.arn,
 *     },
 *     channelName: "example",
 *     clusterArn: exampleAwsMskCluster.arn,
 * });
 * ```
 *
 * ### Apache Iceberg Destination
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.msk.Channel("example", {
 *     topicConfiguration: {
 *         recordConverter: {
 *             valueConverter: "JSON",
 *         },
 *         recordSchema: {
 *             gsrArn: exampleAwsGlueSchema.arn,
 *         },
 *         topicArn: exampleAwsMskTopic.arn,
 *     },
 *     icebergDestination: {
 *         catalog: {
 *             warehouseLocation: exampleAwsS3tablesTableBucket.arn,
 *         },
 *         deadLetterQueueS3: {
 *             bucketArn: dlq.arn,
 *         },
 *         destinationTable: {
 *             partitionSpec: {
 *                 sources: [{
 *                     sourceName: "event_time",
 *                 }],
 *                 partitionStrategy: "TIME_HOUR",
 *             },
 *             destinationDatabaseName: "example_namespace",
 *             destinationTableName: "example_table",
 *         },
 *         schemaEvolution: {
 *             enableSchemaEvolution: false,
 *         },
 *         tableCreation: {
 *             enableTableCreation: true,
 *         },
 *         appendOnly: true,
 *         serviceExecutionRoleArn: exampleAwsIamRole.arn,
 *     },
 *     channelName: "example",
 *     clusterArn: exampleAwsMskCluster.arn,
 * });
 * ```
 *
 * ## Import
 *
 * ### Identity Schema
 *
 * #### Required
 *
 * * `arn` (String) ARN of the channel.
 * * `clusterArn` (String) ARN that uniquely identifies the cluster.
 *
 * #### Optional
 *
 * * `accountId` (String) AWS Account where this resource is managed.
 * * `region` (String) Region where this resource is managed.
 *
 * Using `pulumi import`, import Managed Streaming for Kafka Channel using the `arn` and `clusterArn`. For example:
 *
 * ```sh
 * $ pulumi import aws:msk/channel:Channel example arn:aws:kafka:us-west-2:123456789012:channel/example/279c0212-d057-4dba-9aa9-1c4e5a25bfc7-3/a1b2c3d4,arn:aws:kafka:us-west-2:123456789012:cluster/example/279c0212-d057-4dba-9aa9-1c4e5a25bfc7-3
 * ```
 */
export class Channel extends pulumi.CustomResource {
    /**
     * Get an existing Channel resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ChannelState, opts?: pulumi.CustomResourceOptions): Channel {
        return new Channel(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aws:msk/channel:Channel';

    /**
     * Returns true if the given object is an instance of Channel.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Channel {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Channel.__pulumiType;
    }

    /**
     * ARN of the channel.
     */
    declare public /*out*/ readonly arn: pulumi.Output<string>;
    /**
     * Name of the channel. Must be unique within the cluster. Changing this forces a new resource to be created.
     */
    declare public readonly channelName: pulumi.Output<string>;
    /**
     * ARN that uniquely identifies the cluster. Changing this forces a new resource to be created.
     */
    declare public readonly clusterArn: pulumi.Output<string>;
    /**
     * Type of destination configured for the channel.
     */
    declare public /*out*/ readonly destinationType: pulumi.Output<string>;
    /**
     * AWS KMS encryption configuration applied to data at rest. Changing this forces a new resource to be created. See `encryptionConfiguration` Block below.
     */
    declare public readonly encryptionConfiguration: pulumi.Output<outputs.msk.ChannelEncryptionConfiguration | undefined>;
    /**
     * Apache Iceberg destination for the channel. Exactly one of `icebergDestination` or `s3Destination` is required. With the exception of `dataFreshnessInSeconds`, changing an argument in this block forces a new resource to be created. See `icebergDestination` Block below.
     */
    declare public readonly icebergDestination: pulumi.Output<outputs.msk.ChannelIcebergDestination | undefined>;
    /**
     * Destinations to which the channel publishes operational logs. Changing this forces a new resource to be created. See `loggingInfo` Block below.
     */
    declare public readonly loggingInfo: pulumi.Output<outputs.msk.ChannelLoggingInfo | undefined>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    declare public readonly region: pulumi.Output<string>;
    /**
     * Amazon S3 destination for the channel. Exactly one of `icebergDestination` or `s3Destination` is required. With the exception of `dataFreshnessInSeconds`, changing an argument in this block forces a new resource to be created. See `s3Destination` Block below.
     */
    declare public readonly s3Destination: pulumi.Output<outputs.msk.ChannelS3Destination | undefined>;
    /**
     * Map of tags assigned to the resource. If configured with a provider `defaultTags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
     */
    declare public readonly tags: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * Map of tags assigned to the resource, including those inherited from the provider `defaultTags` configuration block.
     */
    declare public /*out*/ readonly tagsAll: pulumi.Output<{[key: string]: string}>;
    declare public readonly timeouts: pulumi.Output<outputs.msk.ChannelTimeouts | undefined>;
    /**
     * Configuration of the Apache Kafka topic that feeds the channel. Changing this forces a new resource to be created. See `topicConfiguration` Block below.
     *
     * The following arguments are optional:
     */
    declare public readonly topicConfiguration: pulumi.Output<outputs.msk.ChannelTopicConfiguration>;

    /**
     * Create a Channel resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ChannelArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ChannelArgs | ChannelState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ChannelState | undefined;
            resourceInputs["arn"] = state?.arn;
            resourceInputs["channelName"] = state?.channelName;
            resourceInputs["clusterArn"] = state?.clusterArn;
            resourceInputs["destinationType"] = state?.destinationType;
            resourceInputs["encryptionConfiguration"] = state?.encryptionConfiguration;
            resourceInputs["icebergDestination"] = state?.icebergDestination;
            resourceInputs["loggingInfo"] = state?.loggingInfo;
            resourceInputs["region"] = state?.region;
            resourceInputs["s3Destination"] = state?.s3Destination;
            resourceInputs["tags"] = state?.tags;
            resourceInputs["tagsAll"] = state?.tagsAll;
            resourceInputs["timeouts"] = state?.timeouts;
            resourceInputs["topicConfiguration"] = state?.topicConfiguration;
        } else {
            const args = argsOrState as ChannelArgs | undefined;
            if (args?.channelName === undefined && !opts.urn) {
                throw new Error("Missing required property 'channelName'");
            }
            if (args?.clusterArn === undefined && !opts.urn) {
                throw new Error("Missing required property 'clusterArn'");
            }
            if (args?.topicConfiguration === undefined && !opts.urn) {
                throw new Error("Missing required property 'topicConfiguration'");
            }
            resourceInputs["channelName"] = args?.channelName;
            resourceInputs["clusterArn"] = args?.clusterArn;
            resourceInputs["encryptionConfiguration"] = args?.encryptionConfiguration;
            resourceInputs["icebergDestination"] = args?.icebergDestination;
            resourceInputs["loggingInfo"] = args?.loggingInfo;
            resourceInputs["region"] = args?.region;
            resourceInputs["s3Destination"] = args?.s3Destination;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["timeouts"] = args?.timeouts;
            resourceInputs["topicConfiguration"] = args?.topicConfiguration;
            resourceInputs["arn"] = undefined /*out*/;
            resourceInputs["destinationType"] = undefined /*out*/;
            resourceInputs["tagsAll"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Channel.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Channel resources.
 */
export interface ChannelState {
    /**
     * ARN of the channel.
     */
    arn?: pulumi.Input<string | undefined>;
    /**
     * Name of the channel. Must be unique within the cluster. Changing this forces a new resource to be created.
     */
    channelName?: pulumi.Input<string | undefined>;
    /**
     * ARN that uniquely identifies the cluster. Changing this forces a new resource to be created.
     */
    clusterArn?: pulumi.Input<string | undefined>;
    /**
     * Type of destination configured for the channel.
     */
    destinationType?: pulumi.Input<string | undefined>;
    /**
     * AWS KMS encryption configuration applied to data at rest. Changing this forces a new resource to be created. See `encryptionConfiguration` Block below.
     */
    encryptionConfiguration?: pulumi.Input<inputs.msk.ChannelEncryptionConfiguration | undefined>;
    /**
     * Apache Iceberg destination for the channel. Exactly one of `icebergDestination` or `s3Destination` is required. With the exception of `dataFreshnessInSeconds`, changing an argument in this block forces a new resource to be created. See `icebergDestination` Block below.
     */
    icebergDestination?: pulumi.Input<inputs.msk.ChannelIcebergDestination | undefined>;
    /**
     * Destinations to which the channel publishes operational logs. Changing this forces a new resource to be created. See `loggingInfo` Block below.
     */
    loggingInfo?: pulumi.Input<inputs.msk.ChannelLoggingInfo | undefined>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * Amazon S3 destination for the channel. Exactly one of `icebergDestination` or `s3Destination` is required. With the exception of `dataFreshnessInSeconds`, changing an argument in this block forces a new resource to be created. See `s3Destination` Block below.
     */
    s3Destination?: pulumi.Input<inputs.msk.ChannelS3Destination | undefined>;
    /**
     * Map of tags assigned to the resource. If configured with a provider `defaultTags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
     */
    tags?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * Map of tags assigned to the resource, including those inherited from the provider `defaultTags` configuration block.
     */
    tagsAll?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    timeouts?: pulumi.Input<inputs.msk.ChannelTimeouts | undefined>;
    /**
     * Configuration of the Apache Kafka topic that feeds the channel. Changing this forces a new resource to be created. See `topicConfiguration` Block below.
     *
     * The following arguments are optional:
     */
    topicConfiguration?: pulumi.Input<inputs.msk.ChannelTopicConfiguration | undefined>;
}

/**
 * The set of arguments for constructing a Channel resource.
 */
export interface ChannelArgs {
    /**
     * Name of the channel. Must be unique within the cluster. Changing this forces a new resource to be created.
     */
    channelName: pulumi.Input<string>;
    /**
     * ARN that uniquely identifies the cluster. Changing this forces a new resource to be created.
     */
    clusterArn: pulumi.Input<string>;
    /**
     * AWS KMS encryption configuration applied to data at rest. Changing this forces a new resource to be created. See `encryptionConfiguration` Block below.
     */
    encryptionConfiguration?: pulumi.Input<inputs.msk.ChannelEncryptionConfiguration | undefined>;
    /**
     * Apache Iceberg destination for the channel. Exactly one of `icebergDestination` or `s3Destination` is required. With the exception of `dataFreshnessInSeconds`, changing an argument in this block forces a new resource to be created. See `icebergDestination` Block below.
     */
    icebergDestination?: pulumi.Input<inputs.msk.ChannelIcebergDestination | undefined>;
    /**
     * Destinations to which the channel publishes operational logs. Changing this forces a new resource to be created. See `loggingInfo` Block below.
     */
    loggingInfo?: pulumi.Input<inputs.msk.ChannelLoggingInfo | undefined>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * Amazon S3 destination for the channel. Exactly one of `icebergDestination` or `s3Destination` is required. With the exception of `dataFreshnessInSeconds`, changing an argument in this block forces a new resource to be created. See `s3Destination` Block below.
     */
    s3Destination?: pulumi.Input<inputs.msk.ChannelS3Destination | undefined>;
    /**
     * Map of tags assigned to the resource. If configured with a provider `defaultTags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
     */
    tags?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    timeouts?: pulumi.Input<inputs.msk.ChannelTimeouts | undefined>;
    /**
     * Configuration of the Apache Kafka topic that feeds the channel. Changing this forces a new resource to be created. See `topicConfiguration` Block below.
     *
     * The following arguments are optional:
     */
    topicConfiguration: pulumi.Input<inputs.msk.ChannelTopicConfiguration>;
}
