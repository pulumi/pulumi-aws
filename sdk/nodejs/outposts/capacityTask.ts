// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as enums from "../types/enums";
import * as utilities from "../utilities";

/**
 * Resource for managing an AWS Outposts Capacity Task.
 *
 * A capacity task redistributes the instance pools available on an Outpost rack or server to match the `instancePool` configuration declared in the resource. Starting a capacity task is a long-running, asynchronous operation — Terraform waits for it to reach a terminal state (`COMPLETED`, `CANCELLED`, or `FAILED`) before finishing the apply.
 *
 * ## Example Usage
 *
 * ### Minimal
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = aws.outposts.getOutposts({});
 * const exampleCapacityTask = new aws.outposts.CapacityTask("example", {
 *     outpostIdentifier: example.then(example => example.arns?.[0]),
 *     instancePools: [{
 *         instanceType: "m5.large",
 *         count: 2,
 *     }],
 * });
 * ```
 *
 * ### Multiple instance pools, excluded instances, and a specified blocking-instance action
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = aws.outposts.getAssets({
 *     arn: "arn:aws:outposts:us-west-2:123456789012:outpost/op-1234567890abcdef",
 * });
 * const exampleCapacityTask = new aws.outposts.CapacityTask("example", {
 *     outpostIdentifier: "op-1234567890abcdef",
 *     taskActionOnBlockingInstances: "WAIT_FOR_EVACUATION",
 *     assetId: example.then(example => example.assetIds?.[0]),
 *     instancePools: [
 *         {
 *             instanceType: "m5.large",
 *             count: 4,
 *         },
 *         {
 *             instanceType: "c5.xlarge",
 *             count: 2,
 *         },
 *     ],
 *     instancesToExclude: {
 *         instances: [
 *             "i-0123456789abcdef0",
 *             "i-0fedcba9876543210",
 *         ],
 *     },
 * });
 * ```
 *
 * ## Lifecycle
 *
 * Because every argument of this resource is marked as forces-new, any change to the configuration results in destroying and re-creating the capacity task. Tasks that are already in a terminal state (`COMPLETED` or `CANCELLED`) are left in place on destroy and only removed from Terraform state; tasks still in flight are cancelled and Terraform waits for them to reach `CANCELLED`. If a task reaches the terminal state `FAILED` during `delete`, the provider tolerates the "already in a terminal state" error returned by `CancelCapacityTask` and considers the resource successfully destroyed.
 *
 * If a create operation produces a `FAILED` task, the resource is not written to Terraform state (the `failureReason` is surfaced in the diagnostic instead), so no follow-up destroy is required.
 *
 * ## Import
 *
 * ### Identity Schema
 *
 * #### Required
 *
 * * `outpostIdentifier` (String) Outpost identifier supplied when the task was created (ID or ARN).
 * * `capacityTaskId` (String) AWS-assigned capacity task ID.
 *
 * #### Optional
 *
 * * `accountId` (String) AWS Account where this resource is managed.
 * * `region` (String) Region where this resource is managed.
 *
 * Using `pulumi import`, import a Capacity Task using the same composite ID. For example:
 *
 * ```sh
 * $ pulumi import aws:outposts/capacityTask:CapacityTask example op-1234567890abcdef/cap-1a2b3c4d5e6f7g8h9
 * ```
 */
export class CapacityTask extends pulumi.CustomResource {
    /**
     * Get an existing CapacityTask resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: CapacityTaskState, opts?: pulumi.CustomResourceOptions): CapacityTask {
        return new CapacityTask(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aws:outposts/capacityTask:CapacityTask';

    /**
     * Returns true if the given object is an instance of CapacityTask.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is CapacityTask {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === CapacityTask.__pulumiType;
    }

    /**
     * ID of a specific Outposts asset (hardware server) to target for the capacity task. If omitted, AWS selects an appropriate asset automatically. Discover valid asset IDs with the `aws.outposts.getAssets` data source. Changing this value forces a new resource.
     */
    declare public readonly assetId: pulumi.Output<string>;
    /**
     * ID assigned by AWS to the capacity task (for example, `cap-1a2b3c4d5e6f7g8h9`).
     */
    declare public /*out*/ readonly capacityTaskId: pulumi.Output<string>;
    /**
     * RFC 3339 timestamp at which the capacity task reached a terminal state (if any).
     */
    declare public /*out*/ readonly completionDate: pulumi.Output<string>;
    /**
     * RFC 3339 timestamp at which the capacity task was created.
     */
    declare public /*out*/ readonly creationDate: pulumi.Output<string>;
    /**
     * Human-readable reason reported by AWS when the capacity task failed. `null` unless the terminal state is `FAILED`.
     */
    declare public /*out*/ readonly failureReason: pulumi.Output<string>;
    /**
     * One or more `instancePool` blocks defining the desired instance-type layout for the Outpost. See below. At least one block is required. Changing any value forces a new resource.
     */
    declare public readonly instancePools: pulumi.Output<outputs.outposts.CapacityTaskInstancePool[] | undefined>;
    /**
     * Single `instancesToExclude` block specifying user-owned running instances that must not be stopped to free up capacity. See below. Note: AWS does not return this value via the Get/Describe API; after import, you must add the block back to your configuration manually — see Import.
     */
    declare public readonly instancesToExclude: pulumi.Output<outputs.outposts.CapacityTaskInstancesToExclude | undefined>;
    /**
     * ID of the Amazon Web Services Outposts order associated with the capacity task. Changing this value forces a new resource.
     */
    declare public readonly orderId: pulumi.Output<string>;
    /**
     * ID or ARN of the Outpost on which to run the capacity task. Both forms are accepted; the provider normalizes the value internally. Changing this value forces a new resource.
     */
    declare public readonly outpostIdentifier: pulumi.Output<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    declare public readonly region: pulumi.Output<string>;
    /**
     * Current status of the capacity task. One of `REQUESTED`, `IN_PROGRESS`, `WAITING_FOR_EVACUATION`, `CANCELLATION_IN_PROGRESS`, `COMPLETED`, `CANCELLED`, or `FAILED`. See the [AWS documentation](https://docs.aws.amazon.com/outposts/latest/APIReference/API_GetCapacityTask.html) for semantics.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;
    /**
     * Action to take if running instances block the capacity task. Valid values are `WAIT_FOR_EVACUATION` and `FAIL_TASK`. Changing this value forces a new resource.
     */
    declare public readonly taskActionOnBlockingInstances: pulumi.Output<string>;
    /**
     * Configuration block with timeouts. See below.
     */
    declare public readonly timeouts: pulumi.Output<outputs.outposts.CapacityTaskTimeouts | undefined>;

    /**
     * Create a CapacityTask resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: CapacityTaskArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: CapacityTaskArgs | CapacityTaskState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as CapacityTaskState | undefined;
            resourceInputs["assetId"] = state?.assetId;
            resourceInputs["capacityTaskId"] = state?.capacityTaskId;
            resourceInputs["completionDate"] = state?.completionDate;
            resourceInputs["creationDate"] = state?.creationDate;
            resourceInputs["failureReason"] = state?.failureReason;
            resourceInputs["instancePools"] = state?.instancePools;
            resourceInputs["instancesToExclude"] = state?.instancesToExclude;
            resourceInputs["orderId"] = state?.orderId;
            resourceInputs["outpostIdentifier"] = state?.outpostIdentifier;
            resourceInputs["region"] = state?.region;
            resourceInputs["status"] = state?.status;
            resourceInputs["taskActionOnBlockingInstances"] = state?.taskActionOnBlockingInstances;
            resourceInputs["timeouts"] = state?.timeouts;
        } else {
            const args = argsOrState as CapacityTaskArgs | undefined;
            if (args?.outpostIdentifier === undefined && !opts.urn) {
                throw new Error("Missing required property 'outpostIdentifier'");
            }
            resourceInputs["assetId"] = args?.assetId;
            resourceInputs["instancePools"] = args?.instancePools;
            resourceInputs["instancesToExclude"] = args?.instancesToExclude;
            resourceInputs["orderId"] = args?.orderId;
            resourceInputs["outpostIdentifier"] = args?.outpostIdentifier;
            resourceInputs["region"] = args?.region;
            resourceInputs["taskActionOnBlockingInstances"] = args?.taskActionOnBlockingInstances;
            resourceInputs["timeouts"] = args?.timeouts;
            resourceInputs["capacityTaskId"] = undefined /*out*/;
            resourceInputs["completionDate"] = undefined /*out*/;
            resourceInputs["creationDate"] = undefined /*out*/;
            resourceInputs["failureReason"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(CapacityTask.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering CapacityTask resources.
 */
export interface CapacityTaskState {
    /**
     * ID of a specific Outposts asset (hardware server) to target for the capacity task. If omitted, AWS selects an appropriate asset automatically. Discover valid asset IDs with the `aws.outposts.getAssets` data source. Changing this value forces a new resource.
     */
    assetId?: pulumi.Input<string | undefined>;
    /**
     * ID assigned by AWS to the capacity task (for example, `cap-1a2b3c4d5e6f7g8h9`).
     */
    capacityTaskId?: pulumi.Input<string | undefined>;
    /**
     * RFC 3339 timestamp at which the capacity task reached a terminal state (if any).
     */
    completionDate?: pulumi.Input<string | undefined>;
    /**
     * RFC 3339 timestamp at which the capacity task was created.
     */
    creationDate?: pulumi.Input<string | undefined>;
    /**
     * Human-readable reason reported by AWS when the capacity task failed. `null` unless the terminal state is `FAILED`.
     */
    failureReason?: pulumi.Input<string | undefined>;
    /**
     * One or more `instancePool` blocks defining the desired instance-type layout for the Outpost. See below. At least one block is required. Changing any value forces a new resource.
     */
    instancePools?: pulumi.Input<pulumi.Input<inputs.outposts.CapacityTaskInstancePool>[] | undefined>;
    /**
     * Single `instancesToExclude` block specifying user-owned running instances that must not be stopped to free up capacity. See below. Note: AWS does not return this value via the Get/Describe API; after import, you must add the block back to your configuration manually — see Import.
     */
    instancesToExclude?: pulumi.Input<inputs.outposts.CapacityTaskInstancesToExclude | undefined>;
    /**
     * ID of the Amazon Web Services Outposts order associated with the capacity task. Changing this value forces a new resource.
     */
    orderId?: pulumi.Input<string | undefined>;
    /**
     * ID or ARN of the Outpost on which to run the capacity task. Both forms are accepted; the provider normalizes the value internally. Changing this value forces a new resource.
     */
    outpostIdentifier?: pulumi.Input<string | undefined>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * Current status of the capacity task. One of `REQUESTED`, `IN_PROGRESS`, `WAITING_FOR_EVACUATION`, `CANCELLATION_IN_PROGRESS`, `COMPLETED`, `CANCELLED`, or `FAILED`. See the [AWS documentation](https://docs.aws.amazon.com/outposts/latest/APIReference/API_GetCapacityTask.html) for semantics.
     */
    status?: pulumi.Input<string | undefined>;
    /**
     * Action to take if running instances block the capacity task. Valid values are `WAIT_FOR_EVACUATION` and `FAIL_TASK`. Changing this value forces a new resource.
     */
    taskActionOnBlockingInstances?: pulumi.Input<string | undefined>;
    /**
     * Configuration block with timeouts. See below.
     */
    timeouts?: pulumi.Input<inputs.outposts.CapacityTaskTimeouts | undefined>;
}

/**
 * The set of arguments for constructing a CapacityTask resource.
 */
export interface CapacityTaskArgs {
    /**
     * ID of a specific Outposts asset (hardware server) to target for the capacity task. If omitted, AWS selects an appropriate asset automatically. Discover valid asset IDs with the `aws.outposts.getAssets` data source. Changing this value forces a new resource.
     */
    assetId?: pulumi.Input<string | undefined>;
    /**
     * One or more `instancePool` blocks defining the desired instance-type layout for the Outpost. See below. At least one block is required. Changing any value forces a new resource.
     */
    instancePools?: pulumi.Input<pulumi.Input<inputs.outposts.CapacityTaskInstancePool>[] | undefined>;
    /**
     * Single `instancesToExclude` block specifying user-owned running instances that must not be stopped to free up capacity. See below. Note: AWS does not return this value via the Get/Describe API; after import, you must add the block back to your configuration manually — see Import.
     */
    instancesToExclude?: pulumi.Input<inputs.outposts.CapacityTaskInstancesToExclude | undefined>;
    /**
     * ID of the Amazon Web Services Outposts order associated with the capacity task. Changing this value forces a new resource.
     */
    orderId?: pulumi.Input<string | undefined>;
    /**
     * ID or ARN of the Outpost on which to run the capacity task. Both forms are accepted; the provider normalizes the value internally. Changing this value forces a new resource.
     */
    outpostIdentifier: pulumi.Input<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * Action to take if running instances block the capacity task. Valid values are `WAIT_FOR_EVACUATION` and `FAIL_TASK`. Changing this value forces a new resource.
     */
    taskActionOnBlockingInstances?: pulumi.Input<string | undefined>;
    /**
     * Configuration block with timeouts. See below.
     */
    timeouts?: pulumi.Input<inputs.outposts.CapacityTaskTimeouts | undefined>;
}
