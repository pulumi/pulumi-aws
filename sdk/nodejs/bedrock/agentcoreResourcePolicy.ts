// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages an AWS Bedrock Agent Core Resource Policy. Resource-based policies in Amazon Bedrock Agent Core allow you to control which principals (AWS accounts, IAM users, or IAM roles) can invoke and manage your Amazon Bedrock Agent Core Runtime and Gateway resources.
 *
 * ## Example Usage
 *
 * ### Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const exampleAgentcoreAgentRuntime = new aws.bedrock.AgentcoreAgentRuntime("example", {});
 * const example = aws.iam.getPolicyDocument({
 *     statements: [{
 *         sid: "AllowOAuthFromVPC",
 *         effect: "Allow",
 *         actions: ["bedrock-agentcore:InvokeAgentRuntime"],
 *         principals: [{
 *             type: "*",
 *             identifiers: ["*"],
 *         }],
 *         resources: [agentRuntime.example.agentRuntimeArn],
 *         conditions: [{
 *             test: "StringEquals",
 *             variable: "aws:SourceVpc",
 *             values: ["vpc-1a2b3c4d"],
 *         }],
 *     }],
 * });
 * const exampleAgentcoreResourcePolicy = new aws.bedrock.AgentcoreResourcePolicy("example", {
 *     policy: example.then(example => example.json),
 *     resourceArn: exampleAgentcoreAgentRuntime.agentRuntimeArn,
 * });
 * ```
 *
 * ## Import
 *
 * ### Identity Schema
 *
 * #### Required
 *
 * * `resourceArn` - ARN of the resource to which the Resource Policy is attached.
 *
 * Using `pulumi import`, import Bedrock Agent Core Resource Policy using the `resourceArn`. For example:
 *
 * ```sh
 * $ pulumi import aws:bedrock/agentcoreResourcePolicy:AgentcoreResourcePolicy example arn:aws:bedrock-agentcore:us-west-2:012345678901:runtime/abcd1234
 * ```
 */
export class AgentcoreResourcePolicy extends pulumi.CustomResource {
    /**
     * Get an existing AgentcoreResourcePolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AgentcoreResourcePolicyState, opts?: pulumi.CustomResourceOptions): AgentcoreResourcePolicy {
        return new AgentcoreResourcePolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aws:bedrock/agentcoreResourcePolicy:AgentcoreResourcePolicy';

    /**
     * Returns true if the given object is an instance of AgentcoreResourcePolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AgentcoreResourcePolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AgentcoreResourcePolicy.__pulumiType;
    }

    /**
     * Resource policy definition
     */
    declare public readonly policy: pulumi.Output<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    declare public readonly region: pulumi.Output<string>;
    /**
     * Amazon Resource Name (ARN) of the resource for which to create or update the resource policy.
     *
     * The following arguments are optional:
     */
    declare public readonly resourceArn: pulumi.Output<string>;

    /**
     * Create a AgentcoreResourcePolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AgentcoreResourcePolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AgentcoreResourcePolicyArgs | AgentcoreResourcePolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AgentcoreResourcePolicyState | undefined;
            resourceInputs["policy"] = state?.policy;
            resourceInputs["region"] = state?.region;
            resourceInputs["resourceArn"] = state?.resourceArn;
        } else {
            const args = argsOrState as AgentcoreResourcePolicyArgs | undefined;
            if (args?.policy === undefined && !opts.urn) {
                throw new Error("Missing required property 'policy'");
            }
            if (args?.resourceArn === undefined && !opts.urn) {
                throw new Error("Missing required property 'resourceArn'");
            }
            resourceInputs["policy"] = args?.policy;
            resourceInputs["region"] = args?.region;
            resourceInputs["resourceArn"] = args?.resourceArn;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AgentcoreResourcePolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AgentcoreResourcePolicy resources.
 */
export interface AgentcoreResourcePolicyState {
    /**
     * Resource policy definition
     */
    policy?: pulumi.Input<string | undefined>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * Amazon Resource Name (ARN) of the resource for which to create or update the resource policy.
     *
     * The following arguments are optional:
     */
    resourceArn?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a AgentcoreResourcePolicy resource.
 */
export interface AgentcoreResourcePolicyArgs {
    /**
     * Resource policy definition
     */
    policy: pulumi.Input<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * Amazon Resource Name (ARN) of the resource for which to create or update the resource policy.
     *
     * The following arguments are optional:
     */
    resourceArn: pulumi.Input<string>;
}
