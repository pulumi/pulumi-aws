// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as enums from "../types/enums";
import * as utilities from "../utilities";

/**
 * Manages an AWS CloudWatch Observability Admin Telemetry Enrichment.
 *
 * Telemetry enrichment enables resource tags for telemetry data in your account, enhancing telemetry with additional resource metadata from AWS Resource Explorer to provide richer context for monitoring and observability.
 *
 * For more information, see the [AWS CloudWatch Observability Admin documentation](https://docs.aws.amazon.com/cloudwatch/latest/observabilityadmin/what-is-observabilityadmin.html).
 *
 * > **NOTE:** Only one telemetry enrichment can exist per account per region. Creating this resource enables the feature; destroying it disables the feature.
 *
 * ## Example Usage
 *
 * ### Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.observabilityadmin.TelemetryEnrichment("example", {});
 * ```
 *
 * ## Import
 *
 * ### Identity Schema
 *
 * #### Optional
 *
 * * `accountId` (String) AWS Account where this resource is managed.
 * * `region` (String) Region where this resource is managed.
 *
 * Using `pulumi import`, import CloudWatch Observability Admin Telemetry Enrichment using the region name. For example:
 *
 * ```sh
 * $ pulumi import aws:observabilityadmin/telemetryEnrichment:TelemetryEnrichment example us-west-2
 * ```
 */
export class TelemetryEnrichment extends pulumi.CustomResource {
    /**
     * Get an existing TelemetryEnrichment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: TelemetryEnrichmentState, opts?: pulumi.CustomResourceOptions): TelemetryEnrichment {
        return new TelemetryEnrichment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aws:observabilityadmin/telemetryEnrichment:TelemetryEnrichment';

    /**
     * Returns true if the given object is an instance of TelemetryEnrichment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is TelemetryEnrichment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === TelemetryEnrichment.__pulumiType;
    }

    /**
     * ARN of the AWS Resource Explorer managed view created for the telemetry enrichment feature.
     */
    declare public /*out*/ readonly awsResourceExplorerManagedViewArn: pulumi.Output<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    declare public readonly region: pulumi.Output<string>;
    declare public readonly timeouts: pulumi.Output<outputs.observabilityadmin.TelemetryEnrichmentTimeouts | undefined>;

    /**
     * Create a TelemetryEnrichment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: TelemetryEnrichmentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: TelemetryEnrichmentArgs | TelemetryEnrichmentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as TelemetryEnrichmentState | undefined;
            resourceInputs["awsResourceExplorerManagedViewArn"] = state?.awsResourceExplorerManagedViewArn;
            resourceInputs["region"] = state?.region;
            resourceInputs["timeouts"] = state?.timeouts;
        } else {
            const args = argsOrState as TelemetryEnrichmentArgs | undefined;
            resourceInputs["region"] = args?.region;
            resourceInputs["timeouts"] = args?.timeouts;
            resourceInputs["awsResourceExplorerManagedViewArn"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(TelemetryEnrichment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering TelemetryEnrichment resources.
 */
export interface TelemetryEnrichmentState {
    /**
     * ARN of the AWS Resource Explorer managed view created for the telemetry enrichment feature.
     */
    awsResourceExplorerManagedViewArn?: pulumi.Input<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string>;
    timeouts?: pulumi.Input<inputs.observabilityadmin.TelemetryEnrichmentTimeouts>;
}

/**
 * The set of arguments for constructing a TelemetryEnrichment resource.
 */
export interface TelemetryEnrichmentArgs {
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string>;
    timeouts?: pulumi.Input<inputs.observabilityadmin.TelemetryEnrichmentTimeouts>;
}
