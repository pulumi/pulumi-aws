// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as enums from "../types/enums";
import * as utilities from "../utilities";

/**
 * Lists AWS Account Access Entitlements for a given Application.
 *
 * ## Example Usage
 *
 * ### Filter by Principal
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = aws.accountaccess.getEntitlements({
 *     filter: {
 *         principalRole: {
 *             principal: {
 *                 identityCenter: {
 *                     userId: "11111111-2222-3333-4444-555555555555",
 *                 },
 *             },
 *         },
 *     },
 *     applicationArn: exampleAwsAccountaccessApplication.arn,
 * });
 * ```
 *
 * ### Filter by Target Account
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = aws.accountaccess.getEntitlements({
 *     filter: {
 *         principalRole: {
 *             accountId: "123456789012",
 *         },
 *     },
 *     applicationArn: exampleAwsAccountaccessApplication.arn,
 * });
 * ```
 */
export function getEntitlements(args: GetEntitlementsArgs, opts?: pulumi.InvokeOptions): Promise<GetEntitlementsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("aws:accountaccess/getEntitlements:getEntitlements", {
        "applicationArn": args.applicationArn,
        "filter": args.filter,
        "region": args.region,
    }, opts);
}

/**
 * A collection of arguments for invoking getEntitlements.
 */
export interface GetEntitlementsArgs {
    /**
     * ARN of the parent Application to list Entitlements within.
     */
    applicationArn: string;
    /**
     * Filter criteria to narrow the entitlements returned. You can filter by principal, IAM role, or account. See `filter` Block below.
     *
     * The following arguments are optional:
     */
    filter: inputs.accountaccess.GetEntitlementsFilter;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: string;
}

/**
 * A collection of values returned by getEntitlements.
 */
export interface GetEntitlementsResult {
    readonly applicationArn: string;
    /**
     * List of matching Entitlements. See `entitlements` Block below.
     */
    readonly entitlements: outputs.accountaccess.GetEntitlementsEntitlement[];
    readonly filter: outputs.accountaccess.GetEntitlementsFilter;
    readonly region: string;
}
/**
 * Lists AWS Account Access Entitlements for a given Application.
 *
 * ## Example Usage
 *
 * ### Filter by Principal
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = aws.accountaccess.getEntitlements({
 *     filter: {
 *         principalRole: {
 *             principal: {
 *                 identityCenter: {
 *                     userId: "11111111-2222-3333-4444-555555555555",
 *                 },
 *             },
 *         },
 *     },
 *     applicationArn: exampleAwsAccountaccessApplication.arn,
 * });
 * ```
 *
 * ### Filter by Target Account
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = aws.accountaccess.getEntitlements({
 *     filter: {
 *         principalRole: {
 *             accountId: "123456789012",
 *         },
 *     },
 *     applicationArn: exampleAwsAccountaccessApplication.arn,
 * });
 * ```
 */
export function getEntitlementsOutput(args: GetEntitlementsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetEntitlementsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("aws:accountaccess/getEntitlements:getEntitlements", {
        "applicationArn": args.applicationArn,
        "filter": args.filter,
        "region": args.region,
    }, opts);
}

/**
 * A collection of arguments for invoking getEntitlements.
 */
export interface GetEntitlementsOutputArgs {
    /**
     * ARN of the parent Application to list Entitlements within.
     */
    applicationArn: pulumi.Input<string>;
    /**
     * Filter criteria to narrow the entitlements returned. You can filter by principal, IAM role, or account. See `filter` Block below.
     *
     * The following arguments are optional:
     */
    filter: pulumi.Input<inputs.accountaccess.GetEntitlementsFilterArgs>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
}
