// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to manage an S3 Access Grants instance resource policy.
 * Use a resource policy to manage cross-account access to your S3 Access Grants instance.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.s3control.AccessGrantsInstance("example", {});
 * const exampleAccessGrantsInstanceResourcePolicy = new aws.s3control.AccessGrantsInstanceResourcePolicy("example", {policy: pulumi.interpolate`{
 *   "Version": "2012-10-17",
 *   "Id": "S3AccessGrantsPolicy",
 *   "Statement": [{
 *     "Sid": "AllowAccessToS3AccessGrants",
 *     "Effect": "Allow",
 *     "Principal": {
 *       "AWS": "123456789456"
 *     },
 *     "Action": [
 *       "s3:ListAccessGrants",
 *       "s3:ListAccessGrantsLocations",
 *       "s3:GetDataAccess"
 *     ],
 *     "Resource": "${example.accessGrantsInstanceArn}"
 *   }]
 * }
 * `});
 * ```
 *
 * ## Import
 *
 * Using `pulumi import`, import S3 Access Grants instance resource policies using the `account_id`. For example:
 *
 * ```sh
 * $ pulumi import aws:s3control/accessGrantsInstanceResourcePolicy:AccessGrantsInstanceResourcePolicy example 123456789012
 * ```
 */
export class AccessGrantsInstanceResourcePolicy extends pulumi.CustomResource {
    /**
     * Get an existing AccessGrantsInstanceResourcePolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AccessGrantsInstanceResourcePolicyState, opts?: pulumi.CustomResourceOptions): AccessGrantsInstanceResourcePolicy {
        return new AccessGrantsInstanceResourcePolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aws:s3control/accessGrantsInstanceResourcePolicy:AccessGrantsInstanceResourcePolicy';

    /**
     * Returns true if the given object is an instance of AccessGrantsInstanceResourcePolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AccessGrantsInstanceResourcePolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AccessGrantsInstanceResourcePolicy.__pulumiType;
    }

    public readonly accountId!: pulumi.Output<string>;
    /**
     * The policy document.
     */
    public readonly policy!: pulumi.Output<string>;

    /**
     * Create a AccessGrantsInstanceResourcePolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AccessGrantsInstanceResourcePolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AccessGrantsInstanceResourcePolicyArgs | AccessGrantsInstanceResourcePolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AccessGrantsInstanceResourcePolicyState | undefined;
            resourceInputs["accountId"] = state ? state.accountId : undefined;
            resourceInputs["policy"] = state ? state.policy : undefined;
        } else {
            const args = argsOrState as AccessGrantsInstanceResourcePolicyArgs | undefined;
            if ((!args || args.policy === undefined) && !opts.urn) {
                throw new Error("Missing required property 'policy'");
            }
            resourceInputs["accountId"] = args ? args.accountId : undefined;
            resourceInputs["policy"] = args ? args.policy : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AccessGrantsInstanceResourcePolicy.__pulumiType, name, resourceInputs, opts, false /*remote*/);
    }
}

/**
 * Input properties used for looking up and filtering AccessGrantsInstanceResourcePolicy resources.
 */
export interface AccessGrantsInstanceResourcePolicyState {
    accountId?: pulumi.Input<string>;
    /**
     * The policy document.
     */
    policy?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a AccessGrantsInstanceResourcePolicy resource.
 */
export interface AccessGrantsInstanceResourcePolicyArgs {
    accountId?: pulumi.Input<string>;
    /**
     * The policy document.
     */
    policy: pulumi.Input<string>;
}
