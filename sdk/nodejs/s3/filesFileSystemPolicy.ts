// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages an S3 Files File System Policy.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const current = aws.getCallerIdentity({});
 * const currentGetPartition = aws.getPartition({});
 * const example = new aws.s3.FilesFileSystemPolicy("example", {
 *     fileSystemId: exampleAwsS3filesFileSystem.id,
 *     policy: JSON.stringify({
 *         Version: "2012-10-17",
 *         Statement: [{
 *             Effect: "Allow",
 *             Principal: {
 *                 AWS: Promise.all([currentGetPartition, current]).then(([currentGetPartition, current]) => `arn:${currentGetPartition.partition}:iam::${current.accountId}:root`),
 *             },
 *             Action: "s3files:ClientMount",
 *             Resource: "*",
 *         }],
 *     }),
 * });
 * ```
 *
 * ## Import
 *
 * ### Identity Schema
 *
 * #### Required
 *
 * * `fileSystemId` - File system ID.
 *
 * #### Optional
 *
 * * `accountId` (String) AWS Account where this resource is managed.
 * * `region` (String) Region where this resource is managed.
 *
 * Using `pulumi import`, import S3 Files File System Policy using `fileSystemId`. For example:
 *
 * ```sh
 * $ pulumi import aws:s3/filesFileSystemPolicy:FilesFileSystemPolicy example fs-1234567890abcdef0
 * ```
 */
export class FilesFileSystemPolicy extends pulumi.CustomResource {
    /**
     * Get an existing FilesFileSystemPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: FilesFileSystemPolicyState, opts?: pulumi.CustomResourceOptions): FilesFileSystemPolicy {
        return new FilesFileSystemPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aws:s3/filesFileSystemPolicy:FilesFileSystemPolicy';

    /**
     * Returns true if the given object is an instance of FilesFileSystemPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is FilesFileSystemPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === FilesFileSystemPolicy.__pulumiType;
    }

    /**
     * File system ID. Changing this value forces replacement.
     */
    declare public readonly fileSystemId: pulumi.Output<string>;
    /**
     * JSON policy document.
     *
     * The following arguments are optional:
     */
    declare public readonly policy: pulumi.Output<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    declare public readonly region: pulumi.Output<string>;

    /**
     * Create a FilesFileSystemPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: FilesFileSystemPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: FilesFileSystemPolicyArgs | FilesFileSystemPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as FilesFileSystemPolicyState | undefined;
            resourceInputs["fileSystemId"] = state?.fileSystemId;
            resourceInputs["policy"] = state?.policy;
            resourceInputs["region"] = state?.region;
        } else {
            const args = argsOrState as FilesFileSystemPolicyArgs | undefined;
            if (args?.fileSystemId === undefined && !opts.urn) {
                throw new Error("Missing required property 'fileSystemId'");
            }
            if (args?.policy === undefined && !opts.urn) {
                throw new Error("Missing required property 'policy'");
            }
            resourceInputs["fileSystemId"] = args?.fileSystemId;
            resourceInputs["policy"] = args?.policy;
            resourceInputs["region"] = args?.region;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(FilesFileSystemPolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering FilesFileSystemPolicy resources.
 */
export interface FilesFileSystemPolicyState {
    /**
     * File system ID. Changing this value forces replacement.
     */
    fileSystemId?: pulumi.Input<string>;
    /**
     * JSON policy document.
     *
     * The following arguments are optional:
     */
    policy?: pulumi.Input<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a FilesFileSystemPolicy resource.
 */
export interface FilesFileSystemPolicyArgs {
    /**
     * File system ID. Changing this value forces replacement.
     */
    fileSystemId: pulumi.Input<string>;
    /**
     * JSON policy document.
     *
     * The following arguments are optional:
     */
    policy: pulumi.Input<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string>;
}
