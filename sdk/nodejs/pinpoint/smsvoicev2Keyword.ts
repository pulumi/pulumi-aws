// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages an AWS End User Messaging SMS Keyword.
 *
 * > **Note:** The mandatory keywords `HELP` and `STOP` exist on every origination identity and cannot be created or deleted independently of it. For these mandatory keywords only, this resource adopts and manages the `keywordMessage` in place, while `keywordAction` is managed by AWS and cannot be set. Destroying a resource that manages a mandatory keyword does not delete or reset it in AWS; the keyword remains on the origination identity with its last-applied message. Non-mandatory keywords are created and deleted normally.
 *
 * ## Example Usage
 *
 * ### Phone Number
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.pinpoint.Smsvoicev2PhoneNumber("example", {
 *     isoCountryCode: "US",
 *     messageType: "TRANSACTIONAL",
 *     numberType: "SIMULATOR",
 *     numberCapabilities: ["SMS"],
 * });
 * const exampleSmsvoicev2Keyword = new aws.pinpoint.Smsvoicev2Keyword("example", {
 *     originationIdentityArn: example.arn,
 *     keyword: "EXAMPLE",
 *     keywordMessage: "Thanks for messaging our example number.",
 * });
 * ```
 *
 * ### Pool
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.pinpoint.Smsvoicev2PhoneNumber("example", {
 *     isoCountryCode: "US",
 *     messageType: "TRANSACTIONAL",
 *     numberType: "SIMULATOR",
 *     numberCapabilities: ["SMS"],
 * });
 * const exampleSmsvoicev2Pool = new aws.pinpoint.Smsvoicev2Pool("example", {
 *     isoCountryCode: "US",
 *     messageType: "TRANSACTIONAL",
 *     originationIdentities: [example.arn],
 * });
 * const exampleSmsvoicev2Keyword = new aws.pinpoint.Smsvoicev2Keyword("example", {
 *     originationIdentityArn: exampleSmsvoicev2Pool.arn,
 *     keyword: "OPTOUT",
 *     keywordMessage: "You have been unsubscribed.",
 *     keywordAction: "OPT_OUT",
 * });
 * ```
 *
 * ### Mandatory Keyword
 *
 * The mandatory `HELP` and `STOP` keywords are adopted rather than created. Omit `keywordAction`; AWS manages it.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.pinpoint.Smsvoicev2PhoneNumber("example", {
 *     isoCountryCode: "US",
 *     messageType: "TRANSACTIONAL",
 *     numberType: "SIMULATOR",
 *     numberCapabilities: ["SMS"],
 * });
 * const help = new aws.pinpoint.Smsvoicev2Keyword("help", {
 *     originationIdentityArn: example.arn,
 *     keyword: "HELP",
 *     keywordMessage: "Reply STOP to unsubscribe. Message and data rates may apply.",
 * });
 * ```
 *
 * ## Import
 *
 * ### Identity Schema
 *
 * #### Required
 *
 * * `originationIdentityArn` (String) ARN of the origination identity the keyword is attached to.
 * * `keyword` (String) Keyword text.
 *
 * #### Optional
 *
 * * `accountId` (String) AWS Account where this resource is managed.
 * * `region` (String) Region where this resource is managed.
 *
 * Using `pulumi import`, import a keyword using the `originationIdentityArn` and `keyword`, separated by a comma. For example:
 *
 * ```sh
 * $ pulumi import aws:pinpoint/smsvoicev2Keyword:Smsvoicev2Keyword example "arn:aws:sms-voice:us-east-1:123456789012:phone-number/phone-abcdef0123456789abcdef0123456789,EXAMPLE"
 * ```
 */
export class Smsvoicev2Keyword extends pulumi.CustomResource {
    /**
     * Get an existing Smsvoicev2Keyword resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: Smsvoicev2KeywordState, opts?: pulumi.CustomResourceOptions): Smsvoicev2Keyword {
        return new Smsvoicev2Keyword(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aws:pinpoint/smsvoicev2Keyword:Smsvoicev2Keyword';

    /**
     * Returns true if the given object is an instance of Smsvoicev2Keyword.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Smsvoicev2Keyword {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Smsvoicev2Keyword.__pulumiType;
    }

    /**
     * Keyword to configure. Must be upper-case; AWS stores keywords in upper-case. 1-30 characters and cannot start or end with a space. Changing this forces a new resource.
     */
    declare public readonly keyword: pulumi.Output<string>;
    /**
     * Action to perform when the keyword is received. Valid values: `AUTOMATIC_RESPONSE`, `OPT_OUT`, `OPT_IN`. Defaults to `AUTOMATIC_RESPONSE`. Must not be set for mandatory keywords, whose action is managed by AWS.
     */
    declare public readonly keywordAction: pulumi.Output<string>;
    /**
     * Message to send when the keyword is received.
     */
    declare public readonly keywordMessage: pulumi.Output<string>;
    /**
     * ARN of the origination identity (phone number or pool) to attach the keyword to. Changing this forces a new resource.
     *
     * The following arguments are optional:
     */
    declare public readonly originationIdentityArn: pulumi.Output<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    declare public readonly region: pulumi.Output<string>;

    /**
     * Create a Smsvoicev2Keyword resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: Smsvoicev2KeywordArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: Smsvoicev2KeywordArgs | Smsvoicev2KeywordState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as Smsvoicev2KeywordState | undefined;
            resourceInputs["keyword"] = state?.keyword;
            resourceInputs["keywordAction"] = state?.keywordAction;
            resourceInputs["keywordMessage"] = state?.keywordMessage;
            resourceInputs["originationIdentityArn"] = state?.originationIdentityArn;
            resourceInputs["region"] = state?.region;
        } else {
            const args = argsOrState as Smsvoicev2KeywordArgs | undefined;
            if (args?.keyword === undefined && !opts.urn) {
                throw new Error("Missing required property 'keyword'");
            }
            if (args?.keywordMessage === undefined && !opts.urn) {
                throw new Error("Missing required property 'keywordMessage'");
            }
            if (args?.originationIdentityArn === undefined && !opts.urn) {
                throw new Error("Missing required property 'originationIdentityArn'");
            }
            resourceInputs["keyword"] = args?.keyword;
            resourceInputs["keywordAction"] = args?.keywordAction;
            resourceInputs["keywordMessage"] = args?.keywordMessage;
            resourceInputs["originationIdentityArn"] = args?.originationIdentityArn;
            resourceInputs["region"] = args?.region;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Smsvoicev2Keyword.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Smsvoicev2Keyword resources.
 */
export interface Smsvoicev2KeywordState {
    /**
     * Keyword to configure. Must be upper-case; AWS stores keywords in upper-case. 1-30 characters and cannot start or end with a space. Changing this forces a new resource.
     */
    keyword?: pulumi.Input<string | undefined>;
    /**
     * Action to perform when the keyword is received. Valid values: `AUTOMATIC_RESPONSE`, `OPT_OUT`, `OPT_IN`. Defaults to `AUTOMATIC_RESPONSE`. Must not be set for mandatory keywords, whose action is managed by AWS.
     */
    keywordAction?: pulumi.Input<string | undefined>;
    /**
     * Message to send when the keyword is received.
     */
    keywordMessage?: pulumi.Input<string | undefined>;
    /**
     * ARN of the origination identity (phone number or pool) to attach the keyword to. Changing this forces a new resource.
     *
     * The following arguments are optional:
     */
    originationIdentityArn?: pulumi.Input<string | undefined>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a Smsvoicev2Keyword resource.
 */
export interface Smsvoicev2KeywordArgs {
    /**
     * Keyword to configure. Must be upper-case; AWS stores keywords in upper-case. 1-30 characters and cannot start or end with a space. Changing this forces a new resource.
     */
    keyword: pulumi.Input<string>;
    /**
     * Action to perform when the keyword is received. Valid values: `AUTOMATIC_RESPONSE`, `OPT_OUT`, `OPT_IN`. Defaults to `AUTOMATIC_RESPONSE`. Must not be set for mandatory keywords, whose action is managed by AWS.
     */
    keywordAction?: pulumi.Input<string | undefined>;
    /**
     * Message to send when the keyword is received.
     */
    keywordMessage: pulumi.Input<string>;
    /**
     * ARN of the origination identity (phone number or pool) to attach the keyword to. Changing this forces a new resource.
     *
     * The following arguments are optional:
     */
    originationIdentityArn: pulumi.Input<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
}
