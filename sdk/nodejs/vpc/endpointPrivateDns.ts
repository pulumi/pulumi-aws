// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Resource for enabling private DNS on an AWS VPC (Virtual Private Cloud) Endpoint.
 *
 * > When using this resource, the `privateDnsEnabled` argument should be omitted on the parent `aws.ec2.VpcEndpoint` resource.
 * Setting the value both places can lead to unintended behavior and persistent differences.
 *
 * ## Example Usage
 *
 * ### Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.vpc.EndpointPrivateDns("example", {
 *     vpcEndpointId: exampleAwsVpcEndpoint.id,
 *     privateDnsEnabled: true,
 * });
 * ```
 *
 * ## Import
 *
 * Using `pulumi import`, import a VPC (Virtual Private Cloud) Endpoint Private DNS using the `vpc_endpoint_id`. For example:
 *
 * ```sh
 * $ pulumi import aws:vpc/endpointPrivateDns:EndpointPrivateDns example vpce-abcd-1234
 * ```
 */
export class EndpointPrivateDns extends pulumi.CustomResource {
    /**
     * Get an existing EndpointPrivateDns resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: EndpointPrivateDnsState, opts?: pulumi.CustomResourceOptions): EndpointPrivateDns {
        return new EndpointPrivateDns(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aws:vpc/endpointPrivateDns:EndpointPrivateDns';

    /**
     * Returns true if the given object is an instance of EndpointPrivateDns.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is EndpointPrivateDns {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === EndpointPrivateDns.__pulumiType;
    }

    /**
     * Indicates whether a private hosted zone is associated with the VPC. Only applicable for `Interface` endpoints.
     */
    public readonly privateDnsEnabled!: pulumi.Output<boolean>;
    /**
     * VPC endpoint identifier.
     */
    public readonly vpcEndpointId!: pulumi.Output<string>;

    /**
     * Create a EndpointPrivateDns resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: EndpointPrivateDnsArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: EndpointPrivateDnsArgs | EndpointPrivateDnsState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as EndpointPrivateDnsState | undefined;
            resourceInputs["privateDnsEnabled"] = state ? state.privateDnsEnabled : undefined;
            resourceInputs["vpcEndpointId"] = state ? state.vpcEndpointId : undefined;
        } else {
            const args = argsOrState as EndpointPrivateDnsArgs | undefined;
            if ((!args || args.privateDnsEnabled === undefined) && !opts.urn) {
                throw new Error("Missing required property 'privateDnsEnabled'");
            }
            if ((!args || args.vpcEndpointId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'vpcEndpointId'");
            }
            resourceInputs["privateDnsEnabled"] = args ? args.privateDnsEnabled : undefined;
            resourceInputs["vpcEndpointId"] = args ? args.vpcEndpointId : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(EndpointPrivateDns.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering EndpointPrivateDns resources.
 */
export interface EndpointPrivateDnsState {
    /**
     * Indicates whether a private hosted zone is associated with the VPC. Only applicable for `Interface` endpoints.
     */
    privateDnsEnabled?: pulumi.Input<boolean>;
    /**
     * VPC endpoint identifier.
     */
    vpcEndpointId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a EndpointPrivateDns resource.
 */
export interface EndpointPrivateDnsArgs {
    /**
     * Indicates whether a private hosted zone is associated with the VPC. Only applicable for `Interface` endpoints.
     */
    privateDnsEnabled: pulumi.Input<boolean>;
    /**
     * VPC endpoint identifier.
     */
    vpcEndpointId: pulumi.Input<string>;
}
