// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as enums from "../types/enums";
import * as utilities from "../utilities";

/**
 * Manages the state of the AWS FIS (Fault Injection Simulator) safety lever for the account and Region. The safety lever is a single, account/Region-wide emergency stop: engaging it immediately stops all running experiments and blocks new ones from starting.
 *
 * There is exactly one safety lever per account and Region, and it always exists — AWS does not provide APIs to create or delete it. Because of this, deleting this resource only removes it from Terraform state; it does not change the live value in AWS, so it never risks silently disengaging a safety control.
 *
 * > **Note:** AWS rejects a `reason` change unless `status` is also actually transitioning (for example, `engaged` to `disengaged`). Creating or updating this resource with a `status` that already matches the live safety lever succeeds only if the configured `reason` also matches the live `reason`; otherwise it fails at apply time. Pair a `reason` change with an actual `status` change.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.fis.SafetyLeverState("example", {state: {
 *     status: "disengaged",
 *     reason: "Managed by Pulumi",
 * }});
 * ```
 *
 * ## Import
 *
 * ### Identity Schema
 *
 * #### Optional
 *
 * * `accountId` (String) AWS Account where this resource is managed.
 * * `region` (String) Region where this resource is managed.
 *
 * Using `pulumi import`, import the FIS safety lever state using the Region. For example:
 *
 * ```sh
 * $ pulumi import aws:fis/safetyLeverState:SafetyLeverState example us-west-2
 * ```
 */
export class SafetyLeverState extends pulumi.CustomResource {
    /**
     * Get an existing SafetyLeverState resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SafetyLeverStateState, opts?: pulumi.CustomResourceOptions): SafetyLeverState {
        return new SafetyLeverState(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aws:fis/safetyLeverState:SafetyLeverState';

    /**
     * Returns true if the given object is an instance of SafetyLeverState.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SafetyLeverState {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SafetyLeverState.__pulumiType;
    }

    /**
     * ARN of the safety lever.
     */
    declare public /*out*/ readonly arn: pulumi.Output<string>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    declare public readonly region: pulumi.Output<string>;
    /**
     * State of the safety lever. See below.
     *
     * The following arguments are optional:
     */
    declare public readonly state: pulumi.Output<outputs.fis.SafetyLeverStateState | undefined>;
    declare public readonly timeouts: pulumi.Output<outputs.fis.SafetyLeverStateTimeouts | undefined>;

    /**
     * Create a SafetyLeverState resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: SafetyLeverStateArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SafetyLeverStateArgs | SafetyLeverStateState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SafetyLeverStateState | undefined;
            resourceInputs["arn"] = state?.arn;
            resourceInputs["region"] = state?.region;
            resourceInputs["state"] = state?.state;
            resourceInputs["timeouts"] = state?.timeouts;
        } else {
            const args = argsOrState as SafetyLeverStateArgs | undefined;
            resourceInputs["region"] = args?.region;
            resourceInputs["state"] = args?.state;
            resourceInputs["timeouts"] = args?.timeouts;
            resourceInputs["arn"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SafetyLeverState.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SafetyLeverState resources.
 */
export interface SafetyLeverStateState {
    /**
     * ARN of the safety lever.
     */
    arn?: pulumi.Input<string | undefined>;
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * State of the safety lever. See below.
     *
     * The following arguments are optional:
     */
    state?: pulumi.Input<inputs.fis.SafetyLeverStateState | undefined>;
    timeouts?: pulumi.Input<inputs.fis.SafetyLeverStateTimeouts | undefined>;
}

/**
 * The set of arguments for constructing a SafetyLeverState resource.
 */
export interface SafetyLeverStateArgs {
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * State of the safety lever. See below.
     *
     * The following arguments are optional:
     */
    state?: pulumi.Input<inputs.fis.SafetyLeverStateState | undefined>;
    timeouts?: pulumi.Input<inputs.fis.SafetyLeverStateTimeouts | undefined>;
}
