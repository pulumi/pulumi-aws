#!/usr/bin/env python3
# WARNING: This file is autogenerated - changes will be overwritten when regenerated by https://github.com/pulumi/ci-mgmt
"""Bake the mise global fallback into the image, once tools are installed.

Sorts before the other setup.d hooks so anything they shell out to already
resolves. Failures are fatal here: a missing fallback means the build breaks
later, deep in a build tool, with a much less obvious error.
"""

import os
import sys


sys.path.insert(0, os.environ["OPENINSPECT_ROOT"])

from mise_global_fallback import main  # noqa: E402


if __name__ == "__main__":
    main()
