name: test-aws-1655-legacy
runtime: yaml
description: |
  Change tag `foo` value to `quux`
resources:
  aws-provider:
    type: pulumi:providers:aws
    properties: 
      defaultTags: 
        tags: 
          foo: quux
    defaultProvider: true
  res:
    type: aws:s3:Bucket
outputs:
  actual: ${res.tagsAll}