name: test-aws-1655-legacy
runtime: yaml
description: |
  Add first default tag: `foo: bar``
resources:
  aws-provider:
    type: pulumi:providers:aws
    properties: 
      defaultTags: 
        tags: 
          foo: bar
    defaultProvider: true
  res:
    type: aws:s3:Bucket
outputs:
  actual: ${res.tagsAll}