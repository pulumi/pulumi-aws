name: test-aws-1655
runtime: yaml
description: Check that removing default tags completely doesn't break the bucket.
resources:
  aws-provider:
    type: pulumi:providers:aws
    defaultProvider: true
  my-bucket:
    type: aws:s3:Bucket
outputs:
  bucketTags: ${my-bucket.tags}
